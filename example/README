This is an example project using PSX_App. Uses both the PSX_App class and the PSX_Pad class.

Compile the project by running comp.bat in a CMD window,
and you can clean the project by running clean.bat
